#include <stdio.h>

int main(){
    int x = 0;
    int y = 0;
    while (x < 5){
        /* sem
           vlozte
           blok
           kodu */
        printf("%d%d ", x, y);
        x = x + 1;
    }
    return 0;
}
