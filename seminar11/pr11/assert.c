#include <stdio.h>
#include <assert.h>

#define VELIKOST 10

int main()
{
    int pole[VELIKOST];
    int cislo;
    int index;

    index = -1;
    assert(index >= 0 && index <= VELIKOST);
    pole[index] = cislo;

    return 0;
}
